YAML
services:
  - type: web
    name: mexc-volume-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python MexcBot.py
    envVars:
      - key: PYTHONUNBUFFERED
        value: "true"
    healthCheckPath: /health
    autoDeploy: true
